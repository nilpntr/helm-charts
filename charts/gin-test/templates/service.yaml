apiVersion: v1
kind: Service
metadata:
  name: {{ include "gin-test.fullname" . }}
  labels:
    {{- include "gin-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gin-test.selectorLabels" . | nindent 4 }}
